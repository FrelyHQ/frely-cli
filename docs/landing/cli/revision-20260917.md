# Frely CLI Landing 修订记录 — 2026-09-17

## 范围与决策状态

- 用户授权：修订 P0、P1；继续分析 P2，确认有问题则修复；分析项目定位，并通过对话确认剩余选择。
- 基线：本地 main `4cfe47748d91675a72348e8e7d2215f557c21d18`，工作区干净，领先 origin/main 11 个提交；本轮使用独立 worktree/分支，不推送这些既有未发布提交。
- 线上基线：`release.json.sourceSha=4b82185660eea14c3b8bd31ad5fde85732366dc2`。
- 本轮分支：`fix/landing-revision-20260917`。
- 执行边界：修订页面、相关指引及构建检查；未发布 CLI、未部署页面、未改动 Relay 服务。
- 确认维度：P0/P1 的事实修复与 P2 修复已授权；主受众、首页定位和新的首屏文案仍待对话确认。以下定位候选均为设计判断。
- 技术栈：沿用现有静态 HTML/CSS/JavaScript 与 Node.js 生成器，无框架迁移。

## 事实与来源

| 事实 | 证据 | 能证明的边界 |
| --- | --- | --- |
| 2026-09-17 npm latest 是 0.6.1 | npm registry latest 元数据、官方 0.6.1 tarball 中 package/dist/index.js | 发行包内容；不能证明全部服务端链路可用 |
| 0.6.1 有本机 MCP、Provider 命令，没有 Agent install/invoke 模块与命令处理 | 同一 tarball；本地 src/index.ts 与 src/skill/access.ts 可见未发布实现 | 源码能力与发行能力不同 |
| 基础 doctor 将未配置 MCP 视为可选 | src/diagnostics.ts | doctor 通过不表示已接通远程客户端 |
| 本机 MCP 提供 workspace_info、list_directory 等只读工具 | src/runtime/mcp.ts | 可以设计只读验证任务；本轮未重新执行完整用户授权链 |
| Shell 以当前操作系统用户权限运行，workspace 不是 Shell 沙箱 | README.md、docs/chatgpt-mcp.md、运行时实现 | 不能宣传为仅能执行于隔离沙箱 |
| CLI/本地运行时采用 Apache-2.0；托管 Relay 是独立依赖 | LICENSE、README.md、site 现有说明 | 不能据此宣传整个平台均可独立自托管 |
| 三种连接分别配置 | MCP 授权实现、Provider 模块、Agent 源码路径 | 不应把授权或安装成功混作所有能力就绪 |

官方包：
- https://registry.npmjs.org/frely-cli/latest
- https://registry.npmjs.org/frely-cli/-/frely-cli-0.6.1.tgz

## 本轮修订及验收

| 编号 | 旧行为 | 新行为 | 理由与验收 |
| --- | --- | --- | --- |
| P0-1 | 首屏/元数据将远程 Agent 与已发布能力并列；示例含 npm 0.6.1 不支持的命令 | 双语 Agent 区块明确“开发中”，注明本页核验发行版；移除可执行示例，入口指向发行记录；首屏描述仅保留发布包已有的 MCP 与 Provider 能力 | 安装已核验版本后不再遇到页面推荐的未知命令；未来改成可用须核验新发行包 |
| P0-2 | README Agent 示例未区分发行与源码；MCP 文档状态笼统写“未发布” | 增加发行边界，Agent 示例标为源码预览；MCP 文档区分 CLI 发布、服务端部署与真实调用验证 | 页面、根文档、入门指南的能力状态一致 |
| P1-1 | 安装 → 登录 → doctor，缺少下一步 | 保留基础诊断并解释边界，增加选择工作流；MCP 路径补充设备/目录批准、原样使用 URL、OAuth、在线要求和首次只读结果核对 | 目标客户端实际工具调用返回可与本地目录核对的名称才算成功 |
| P1-2 | Provider 只展示 share/list | 补充通过接入点/API 密钥取得真实模型回复的完成标准 | 列表记录与推理成功分别判断；未编造回复示例 |
| P2-1 | app.js、locale.js、styles.css 使用固定 URL，页面更新可复用旧缓存 | 构建按资产内容产生 v 参数；保留原文件路径供旧 HTML 使用 | 未变内容 URL 稳定；内容变化后所有语言页均请求新 URL |
| P2-2 | 直达隐藏工作流面板缺少选中处理 | 初次加载及 hashchange 激活对应面板；语言链接保留 query/hash | 由安装区直达 Provider，再切换语言，仍能看到对应面板 |
| P1-3 | 首屏主受众和主任务不明确 | 本轮不重写主标题；下方形成定位候选供确认 | 用目标受众理解测试验证，不能把设计判断视为已证实转化结论 |

P2 原始观察：中文页曾显示英文复制状态，语言链接未保留锚点。复核时三个线上资产已与当前源码一致，响应 Cache-Control 为 max-age=600；因此“新 HTML 与旧脚本缓存混用”是与现象一致的原因推断，未保存当时浏览器实际旧响应字节，不能称为已完整复现根因。构建现在阻止新版 HTML 复用旧资产 URL；源码的语言/复制行为通过回归检查。

## 项目定位分析（待确认）

**项目层定位建议：Frely 的开源连接客户端。**

它连接的是几条方向不同的路径，而不只是把三个功能放进一个 CLI：

| 路径 | 受众与任务假设 | 当前证据与首页角色建议 |
| --- | --- | --- |
| 云端 AI → 本机工具 | 已使用远程 AI、愿意运行 CLI，希望 AI 处理电脑中的文件、命令和持续进程的用户 | 已发布命令、任务具体；建议作为当前首页主入口 |
| 本机模型 → Frely | 已运行 Ollama/兼容运行时，并希望通过 Frely 接入点使用该模型的现有用户 | 已发布命令，但有模型、Provider 槽位和平台使用背景门槛；建议作为第二场景 |
| 现有 AI 客户端 → 远程 Agent | 希望继续使用当前客户端与模型配置，同时调用 Frely 托管 Agent 的用户 | 源码可见实现但核验发行包不支持；当前仅介绍开发方向 |
| Network 接入 | 有独立钱包/Network 能力使用任务的用户 | 与以上三条路径的目的和认证不同；是否在独立文档入口展示待确认，首页本轮未扩展 |

“开源”支持审查客户端代码和本机执行边界；云端连接需要 Frely 服务。不能把开源许可当作完全离线、整个平台开源、自动沙箱或永远免费的证据。

**推荐当前首页主任务：帮助使用远程 AI 的用户完成第一次本机工具连接。**

理由是发布能力、可解释的用户问题和可验收结果更一致。这是基于产品实现的设计判断；尚无访谈证明该人群是最大市场，也无转化数据证明比另两条路径更好。

候选文案 v0.1（未确认，未替换到页面）：
- 产品说明：Frely 的开源命令行客户端。
- 中文标题：让云端 AI，用上你的本地工具。
- 中文副标题：通过 MCP，将你的 AI 客户端连接到本地文件、终端命令和常驻进程。由 Frely 处理连接与授权，工具在你的电脑上执行。
- 英文标题：Connect cloud AI to your local tools.
- 英文副标题：Connect your MCP client to local files, shell commands, and persistent processes. Frely handles the connection and authorization; tools run on your computer.
- 主 CTA 候选：连接我的电脑 / Connect my computer → 页面安装区 → 本地 MCP 路径。
- 次级入口：配置指南、GitHub；本地模型共享在正文作为另一条任务入口。
- 待补证据：真实只读调用的输入和返回结果；不使用编造的终端输出。
- 待明确条件：主要支持的目标客户端、托管服务费用/额度及需要披露的使用限制。

## 13 步进度

| 步骤 | 状态与后续 |
| --- | --- |
| 1 任务与指标 | 已有安装 CTA；建议以首次真实工具调用成功为目标，待确认；点击不是使用成功 |
| 2 主受众 | 已提出三类任务人群；主受众待用户选择 |
| 3 问题与替代方案 | 已有任务假设；未取得访谈、工单原话或切换原因 |
| 4 价值与边界 | 已修正发行边界；长期定位和首屏表达待确认 |
| 5 承诺与证据 | 有代码/发行包证据；缺少可公开真实结果演示与用户案例 |
| 6 行动路径 | 本轮补足操作及完成标准；真实新用户端到端执行待验 |
| 7 参考拆解 | 本轮局部修复无需重建风格；未做参考页面调研，不推断效果 |
| 8 内容结构 | 保留原结构并补充下一步；主任务优先级待定位确认 |
| 9 文案 | 双语事实与步骤已修；主标题候选未实施 |
| 10 交互 | 工作流直达和语言路由有代码回归；本轮尚未完成新页面浏览器视觉验收 |
| 11 理解测试 | 未执行目标用户测试；建议让首次访客解释产品、选择工作流、完成只读调用 |
| 12 可访问性 | 保留键盘、焦点、减少动效和无 JS 内容；手机布局、放大及完整视觉检查待验 |
| 13 上线与数据 | 构建、资产缓存与交互检查完成；未部署、未新增统计 SDK、未观察线上转化 |

## 验证记录

- 9/9 Node.js 测试通过：原有 4 项语言入口路由，新增 4 项交互行为和 1 项跨语言资产缓存更新测试。
- app.js、locale.js 语法检查通过。
- 生成根页、英文页、中文页成功；双语翻译键及模板一致性通过构建验证。
- git diff --check 通过。
- 未执行：新的浏览器视觉/移动端验收、真实安装登录授权、完整 Provider 推理、新用户测试、线上转化观察。
- 本轮未发布新版本或站点；这份记录中的“已修”表示工作分支实现完成。

## 对话确认顺序

1. 首页首先吸引谁：希望 AI 使用本机工具的新用户、现有 Frely 用户，还是以远程 Agent 消费为中心的未来用户？
2. 确认第一项后，再确定首个目标客户端、托管服务条件、页面主行动与候选首屏文案。
3. 收集/制作真实调用证据，确认内容版本后完成余下视觉验收与发布安排。

不以无人回答代替确认，不重复询问已授权的事实修复。
